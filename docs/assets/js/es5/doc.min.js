$(document).ready(() => {
  $('.doc-example'); const t = $('.code'); hljs.configure({ useBR: !0, tabReplace: '  ' }), t.each(function (t) { const e = $(this); const a = e.data('code'); let o = e.attr('lang'); o = o ? [o] : ['xml', 'css', 'javascript', 'json']; const i = hljs.highlightAuto(a, o); e.addClass('hljs'), e.html(hljs.fixMarkup(i.value)), e.wrap('<pre></pre>'); }); const e = new ClipboardJS('.btn-clipboard', { target(t) { return t.parentNode.nextElementSibling; } }); e.on('success', (t) => {
    $(t.trigger).attr('title', 'Copied!').tooltip('_fixTitle').tooltip('show')
      .attr('title', 'Copy to clipboard')
      .tooltip('_fixTitle'), t.clearSelection();
  }), e.on('error', (t) => {}), loadHeader(), loadSidebar();
});
